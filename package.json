{
  "name": "angels-bandits",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": ["common", "client", "server"],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p common && tsc -p client && tsc -p server",
    "lint": "biome check .",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
